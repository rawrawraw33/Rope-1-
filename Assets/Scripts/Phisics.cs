using UnityEngine;

public class Physics : MonoBehaviour
{
    private void Start()
    {
        // Устанавливаем фиксированный интервал времени в 0.01 секунды (100 раз в секунду)
        //Time.fixedDeltaTime = 0.1f;
    }
}
